specVersion: "v1beta"
serviceId: "new-react-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "new-react-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
